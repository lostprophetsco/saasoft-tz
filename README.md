# saasoft-tz - Форма управления учетными записями

Тестовое задание для Vue Frontend Developer

## Описание проекта

Необходимо реализовать форму управления учетными записями с возможностью добавления, удаления и валидации записей.

## Техническое задание

### Стек технологий
- Vue.js 3 + Composition API
- TypeScript
- Pinia (стейт менеджер)
- Любой UI фреймворк на выбор

### Функциональные требования

**Страница состоит из 3 блоков:**
1. **Заголовок "Учетные записи"** и кнопка "Добавить" (в одном ряду)
2. **Панель с информацией и иконкой** 
3. **Таблица** со списком учетных записей

**Поля в таблице:**
- **Метка** - текстовое поле, до 50 символов, можно ввести несколько меток через точку с запятой
- **Тип записи** - выпадающий список (LDAP, Локальная)
- **Логин** - текстовое поле, обязательно для заполнения, до 100 символов
- **Пароль** - поле для пароля, обязательно для "Локальная", до 100 символов
- **Действия** - кнопка удаления записи (показывается только для сохраненных записей)

### Как это работает
1. При нажатии кнопки **"+"** в конец таблицы добавляется новая пустая запись **без кнопки удалить**
2. При нажатии кнопки **удаления** запись полностью удаляется (только у сохраненных записей)
3. **Проверка полей:** обязательные поля проверяются когда вы уходите с них
4. **Подсветка ошибок:** поля с ошибками выделяются красной рамкой
5. **Умное отображение полей:**
   - Новая запись: тип не выбран → поле "Пароль" скрыто
   - При выборе "Локальная": поле "Пароль" появляется
   - При выборе "LDAP": поле "Пароль" скрывается
6. **Адаптивная таблица:** если ни у одной записи нет пароля, поле "Логин" расширяется на 2 колонки
7. **Автосохранение:** изменения сохраняются автоматически при валидных данных в обязательных полях (при добавлении новой учетной записи или обновлении любого поля в имеющейся уже)
8. **Хранение данных:** все сохраняется в браузере и восстанавливается после перезагрузки страницы

## Разработка

```sh
npm install
```

### Запуск для разработки

```sh
npm run dev
```

### Сборка для продакшена

```sh
npm run build
```

### Линтинг

```sh
npm run lint
```

## Структура проекта

```
src/
├── components/
│   ├── AppHeader.vue           # Заголовок с кнопкой "Добавить"
│   ├── AppPanel.vue            # Панель с подсказками
│   └── AppTable.vue            # Таблица с учетными записями
├── composables/
│   ├── useAccounts.ts         # Управление учетными записями и localStorage
│   ├── useValidation.ts       # Валидация полей форм
│   └── useAccountForm.ts       # Логика форм и преобразования данных
├── stores/
│   └── accounts.ts            # Pinia store для состояния приложения
├── types/
│   ├── account.ts              # Типы учетных записей (Account, AccountType)
│   └── table.ts                # Интерфейсы для таблицы (EditableAccount, TableProps)
├── App.vue                     # Основной компонент
└── main.ts                     # Точка входа приложения
```

## Результат

Готовый проект должен быть доступен в репозитории с историей коммитов.

### ✅ Реализованный функционал:
- Vue 3 + TypeScript + PrimeVue
- Компонентная архитектура (AppHeader, AppPanel, AppTable)
- Динамическая таблица с inline редактированием
- Валидация полей через `:invalid`
- Двухфлаговая система сохранения (`isReadyForSave` / `isSaved`)
- Динамическое отображение колонки пароля с colspan
- Empty state для пустой таблицы
- Пароль с возможностью просмотра (`toggleMask`)
- Строгая типизация без `any`
- Pinia store для управления состоянием
- localStorage для персистентности данных
- Автоматическое сохранение и восстановление после перезагрузки

### 🏗️ Архитектура:
- **Composables Pattern:** переиспользуемая бизнес-логика (useAccounts, useValidation, useAccountForm)
- **Pinia Store:** централизованное управление учетными записями
- **localStorage:** персистентность данных между сессиями
- **Компоненты:** модульная структура с четным разделением ответственности
- **Типизация:** строгая TypeScript система без `any`
- **Валидация:** реальная проверка полей с визуальной обратной связью

### 📦 Composables:
- **useAccounts:** CRUD операции, работа с localStorage, управление состоянием
- **useValidation:** валидация полей с учетом бизнес-правил
- **useAccountForm:** создание записей, преобразование данных, управление формами
